# git init

## Purpose

Initialize a new Git repository in the current directory.

## Syntax

```bash
git init
```

## Example

```bash
mkdir my_project
cd my_project
git init
```

## Output

Creates a hidden `.git` directory that stores all repository data.

## Notes

- Run once per project.
- Converts a normal folder into a Git repository.
