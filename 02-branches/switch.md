# git switch

## Purpose

Move between branches.

## Examples

Switch branch:

```bash
git switch main
```

Create and switch:

```bash
git switch -c feature-login
```

## Notes

Preferred over `git checkout` for branch switching.
